version: 2

sources:
  - name: raw
    schema: raw_data
    tables:
      - name: articles

models:
  - name: stg_articles
    description: "Cleaned staging model for scraped articles"
    columns:
      - name: url
        tests:
          - not_null